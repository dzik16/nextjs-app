export interface ProfileResponseType {
  success: string
  code: string
  message: string
  token: string
  refreshToken: string
  groupList: string[]
  moduleObjectMemberList: number[]
  userId: string
}
